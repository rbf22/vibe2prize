---
title: "Accenture Master Template"
phase: "draft"
settings:
  canvas: "1920x1080"
  grid: "80x45"
  gap: "0.5rem"
exclusions: "5,3,1,1"
brand: "accenture light"
regions:
  - id: "section-label"
    role: "section-title"
    hint: "Eyebrow or track name anchoring the narrative."
    grid: "1,1,26x1"
  - id: "hero-title"
    role: "primary-title"
    req: true
    hint: "Primary narrative headline."
    grid: "1,2,52x2"
  - id: "hero-subtitle"
    role: "secondary-title"
    hint: "Subtitle that frames the story."
    grid: "1,4,52x1"
  - id: "brand-logo"
    role: "logo"
    type: "image"
    req: true
    hint: "Brand identifier or watermark."
    grid: "3,43,10x1"
  - id: "page-number"
    role: "page-number"
    req: true
    hint: "Slide number indicator."
    grid: "68,43,9x1"
  - id: "footer"
    role: "footer"
    req: true
    hint: "Legal copy or confidentiality notice."
    grid: "22,43,32x1"
layout:
  type: "grid-designer"
  template: "Accenture Master Template"
  rows: 45
  columns: 80
  gap: "0.5rem"
---

import { GridDesigner, GridArea, ContentRenderer } from "../../core/layout/components.js";

const REGION_IMPORTANCE = {
  "primary-title": "critical",
  "page-number": "critical",
  footer: "critical",
  logo: "supporting",
};

const regions = frontmatter?.regions ?? [];

const resolveImportance = (role) => REGION_IMPORTANCE[role] || "supporting";

<GridDesigner template={frontmatter?.layout?.template || "Accenture Master Template"}>
  {regions.map((region) => (
    <GridArea
      key={region.id}
      area={region.area}
      contentType={region.role}
      importance={resolveImportance(region.role)}
    >
      <ContentRenderer type={region.role} content={""} />
    </GridArea>
  ))}
</GridDesigner>
